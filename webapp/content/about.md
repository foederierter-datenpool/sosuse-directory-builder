## Federated directory of social support services in Germany

Builds a federated directory by mapping heterogeneous source schemas into a unified target schema.
The directory can be queried in the browser or downloaded as a data snapshot.
This site serves both its users and those interested in the federation process itself.
Toggle "Show federation process" in the top bar to inspect the steps.

The published pipeline files include the final directory, catalog, provenance,
logs and federation artifacts. Raw downloads and lifted RDF are not included.

<img src="webapp/content/logo.png" alt="" style="max-height: 160px; max-width: 360px; margin-top: 2rem; display: block;">
