services:
  pipeline:
    build:
      context: .
      dockerfile: deploy/pipeline.Dockerfile
    init: true
    restart: unless-stopped
    environment:
      GITHUB_PUSH_TOKEN: ${GH_PAGES_TOKEN:-}
    volumes:
      - pipeline-workspace:/workspace

volumes:
  pipeline-workspace:
